class Solution {
    //
    public int maxProduct(int[] nums) {
        if(nums.length == 0){
            return 0;
        }
        int res = Integer.MIN_VALUE, max = 0, min = 0;
        for(int i = 0; i < nums.length; i++){
            if(nums[i] == 0;){
                
            }
        }
    }
}
